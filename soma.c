#include <stdio.h>
int main(void){
	int soma=0; //podemos realizar as declaraçoes diretamente aqui
	int num; //declara a variavel num, pois c e de tipagem estatica
	printf("escolha um numero: ");
	scanf("%d",&soma); //seria o input do py,le do usuario p/ a var num/
	//while (num>0){ 
		//soma=(soma+num);
		//num=(num-1);
	//}
	printf("somou %d\n",soma);
return (0);
}
